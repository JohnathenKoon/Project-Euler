#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <sstream>
#include <cstdio>

using namespace std;

int main() {
    ifstream num_file;
    num_file.open("number.txt", fstream::app);

    vector<vector<int>> vec;

    for(int b = 0; b<)

    num_file.close();
    return 0;
}